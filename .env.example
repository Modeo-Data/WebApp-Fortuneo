# Copy this file to .env and fill in the values

# (Optional) Claude API key for AI explanations — leave as "mock" for demo mode
ANTHROPIC_API_KEY=mock

# (Optional) Change the port the app is exposed on (default: 3000)
PORT=3000

# (Optional) Django secret key — change this in production
DJANGO_SECRET_KEY=change-me-in-production
